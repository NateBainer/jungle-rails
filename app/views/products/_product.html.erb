<article class="product">
  <header>	  <header>
    <%= link_to product do %>	    <%= link_to product do %>
      <%= image_tag product.image.thumb, alt: product.name %>	      <%= image_tag product.image.thumb, alt: product.name %>
      <h4><%= product.name %></h4>	      <h4><%= product.name %></h4>
    <% end %>	    <% end %>
  </header>	  </header>
  <p class="description">	  <p class="description">
    <%= product.description %>	    <%= product.description %>
  </p>	  </p>
  <footer class="actions">	  <footer class="actions">
    <%= button_to add_item_cart_path(product_id: product.id), class: 'btn btn-primary' , method: :post do %>	    <%= button_to add_item_cart_path(product_id: product.id), class: 'btn btn-primary' , method: :post do %>
      <%= fa_icon "shopping-cart", text: 'Add' %>	      <%= fa_icon "shopping-cart", text: 'Add' %>
    <% end %>	    <% end %>
    <%= link_to product, class: 'btn btn-default pull-right' do %>	    <%= link_to product, class: 'btn btn-default pull-right' do %>
      Details &raquo;	      Details &raquo;
    <% end %>	    <% end %>
  </footer>	  </footer>
  <% if product.quantity == 0 %>
    <aside class="sold-out">
      SOLD OUT!
    </aside>
  <% end %>


  <aside class="price">	  <aside class="price">
    <%= humanized_money_with_symbol product.price %>	    <%= humanized_money_with_symbol product.price %>
  </aside>	  </aside>
</article>	</article>
